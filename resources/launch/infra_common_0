# Description: small-sized tests on all configurations.
# Size: small(0).
# Purpose: integrational testing.

time_limit = 20
size = 0
test_groups = common
repeat_each = 3
fail_on_tle = true
save = true
description = Integrational tests
gtest_opts = --gtest_filter=*Test*16CPU*
